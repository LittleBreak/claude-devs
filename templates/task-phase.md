# Phase N：<阶段名称>

> **目标**：<一句话描述本阶段核心目标>
>
> **里程碑**：<里程碑编号> - <里程碑名称>（<达成条件的简要描述>）

---

## 任务编号说明

Phase N 共 X 个任务（N.1–N.X），覆盖 <各任务涵盖的功能领域概述>。

---

## Phase N 执行前置（必须确认）

在开始任何 Task 前，先记录并确认以下信息：

- [ ] 前置 Phase 所有任务已完成且 `pnpm test` 全部通过
- [ ] 所需的占位文件/模块已存在（列出具体文件路径）
- [ ] 所需的目录结构已存在（列出具体目录路径）
- [ ] 路径别名可用（列出具体别名，如 `@main`、`@renderer`、`@shared`）
- [ ] 本阶段只引入 `ARCHITECTURE.md` 与 `ROADMAP.md` 已声明依赖
- [ ] 执行任务后必须更新任务状态：任务成功完成时，将对应的验收标准和交付物清单项标记为 `[x]`（已完成）

---

## 任务依赖关系 & 推荐执行顺序

### 依赖关系图

```
N.1（任务名） ← Phase N 起点
  ├── N.2（任务名）← 依赖说明
  ├── N.3（任务名）← 依赖说明
  └── N.4（任务名）← 依赖说明
        └── N.5（任务名）← 依赖说明
```

### 推荐执行顺序

```
N.1 → N.2 → N.3 → N.4 → N.5
```

- <对执行顺序的简要说明，包括哪些可以并行、哪些必须串行>

---

## TDD 分层策略

> 根据任务特性划分测试策略等级。

### A 类：有可测试行为的任务 → 严格 TDD（Red-Green-Refactor）

适用于：包含业务逻辑、数据处理、状态管理、API 交互等可单元测试行为的任务。

1. **Red**：先编写测试（正常路径 + 边界条件 + 错误处理），运行并确认失败
2. **Green**：实现最小代码使测试通过
3. **Refactor**：重构并再次运行测试确保通过

### B 类：纯配置/UI 骨架 → 验证式测试

适用于：纯配置文件、UI 布局骨架、样式调整等无业务逻辑的任务。

1. 编写验证式测试（组件可渲染、配置可加载、结构正确）
2. 实现功能
3. 运行测试确认通过

> **注意**：B 类不豁免测试，仅豁免严格的 Red-Green-Refactor 流程顺序。仍需编写测试保证可回归。

### 统一留痕要求

- [ ] A 类任务：记录 Red 阶段失败测试名称、Green 阶段通过结果、最终回归结果
- [ ] B 类任务：记录验证式测试通过结果
- [ ] 所有任务：`pnpm test` 通过
- [ ] 测试文件与源文件同目录：`*.test.ts` / `*.test.tsx`

---

## N.1 <任务名称>

**目标**：<一句话描述此任务要达成的目标>

**输入/前置条件**：

- 依赖：<列出依赖的前置任务或 Phase>
- 需读取：<列出需要参考的架构文档章节>
- 当前状态：<描述相关文件/模块的当前状态>

**验证策略**：A 类（严格 TDD） / B 类（验证式测试）

**关键决策**：

| 决策项     | 方案                                         |
| ---------- | -------------------------------------------- |
| <决策项 1> | <具体方案描述，包括技术选型、命名规范、策略等> |
| <决策项 2> | <具体方案描述>                                |
| <决策项 3> | <具体方案描述>                                |

**TDD 要求**：

- [ ] Red：先写测试，确认失败。具体测试用例见下方。
- [ ] Green：实现功能使测试通过
- [ ] Refactor：整理代码结构，测试保持通过

**测试用例设计**（Red 阶段编写）：

```typescript
// === <测试文件路径> ===
// 测试用例按三个维度组织：正常路径、边界条件、错误处理

describe("<模块名>", () => {
  // 正常路径：核心功能验证
  it("<正常场景描述>", () => {
    // ...
  });

  // 边界条件：极端输入、空值、上下限
  it("<边界场景描述>", () => {
    // ...
  });

  // 错误处理：异常输入、权限不足、资源不存在
  it("<错误场景描述>", () => {
    // ...
  });
});
```

**执行步骤**：

1. **（Red）** 编写 `<测试文件路径>`：<测试内容概述>
2. 运行 `pnpm test`，确认全部失败
3. **（Green）** 实现 `<源文件路径>`：<实现内容概述>
4. 运行 `pnpm test`，确认测试通过
5. **（Refactor）** <重构内容概述>，再次运行 `pnpm test` 确认通过

**验收标准**：

- [ ] <验收项 1：文件存在性/功能完整性>
- [ ] <验收项 2：类型安全/接口正确性>
- [ ] <验收项 3：安全/性能要求>
- [ ] TDD 留痕完整：Red 阶段测试失败日志 + Green 阶段通过日志
- [ ] `tsc --noEmit` 无类型错误
- [ ] `pnpm test` 回归通过
- [ ] 提供可复核证据：测试输出 + `tsc --noEmit` 输出

**交付物**：

- [ ] `<源文件路径>`（功能描述）
- [ ] `<测试文件路径>`（对应测试）
- [ ] <其他交付文件>

---

## N.2 <任务名称>

<!-- 重复上方单个任务的模板结构 -->

---

<!-- 更多任务... -->
